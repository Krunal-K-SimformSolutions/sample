/**
 * Contains translated strings for the Common module.
 *
 * @type {Object}
 */
const Common = Object.freeze({
  dash: 'common:dash',
  err: 'common:err',
  errUnexpected: 'common:errUnexpected',
  errPermissionNotGranted: 'common:errPermissionNotGranted',
  errUrlNotSupported: 'common:errUrlNotSupported',
  errUrlNotOpened: 'common:errUrlNotOpened',
  errRequireEmail: 'common:errRequireEmail',
  errValidEmail: 'common:errValidEmail',
  errValidPassword: 'common:errValidPassword'
});

/**
 * Contains translated strings for the Auth module.
 *
 * @type {Object}
 */
const Auth = Object.freeze({});

/**
 * Contains translated strings for the Home module.
 *
 * @type {Object}
 */
const Home = Object.freeze({});

/**
 * Contains translated strings for the Other module.
 *
 * @type {Object}
 */
const Other = Object.freeze({});

/**
 * Export all the generated modules.
 *
 * @type {Object}
 */
export default Object.freeze({
  Common,
  Auth,
  Home,
  Other
});
